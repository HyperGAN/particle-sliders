# Field3D mismatch fails → Music YAML hygiene (Fire #13)

Locked recipe **unchanged**. Failures below are **0/6 solid** (not knife-edge).

| cell | fail mode | Music analogue risk |
|---|---|---|
| `mismatch_content_heavy` | declared ê ∥ û while content dominates; leak_ratio≈0.06 | `leak_*` captions that restates slider axis |
| `mismatch_leak_heavy` | unused ê ≫ û in `a`; leak_ratio≈1.18 | leftover amplitude in poles without gate subtract working |
| `mismatch_cross_declare` | declared_e names content; `a` has unused ê; exam_score≈−0.04 | YAML `leak_*` points at delivery/content, not unused attr |
| `entangled_wide_rows` | c=e=0.85, 5 wide row_scales; exam≈0.84 < cont floor | multi-song batch with equal leftover+content and wild scale mix |

**Do transfer:** PairField ports (divergent/close/unused_e) + sheet leftover — 6/6.

**Do not expect recipe to fix:** wrong `leak_*` declaration. Fix YAML / teacher, not cover/particles.

Under dual-arm: leftover mlp arm must have **correct** declared leak axis; listen tx arm does not exercise this.
