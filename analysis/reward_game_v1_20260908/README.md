# Reward-slider game investigation

[Live combined results](http://192.168.1.90:8888/reward-game-current/)

[Current best and fresh evidence](CURRENT.md) · [Concise experiment record](EXPERIMENTS.md) · [Current operational state](RUNNING.md)

The reusable [CLI](../../conceptmod/textsliders/reward_game/README.md) is implemented.
Initialization verified 48 original FLOAT WAV hashes and exactly rebuilt the saved
reference scorecards. Cache, resume, claims, scorecard accounting, rejection
continuation and small-model merged-forward tests pass. The single Off PCM rerender passed exactly. The known checkpoint was rejected at 8 cases: 5/8 wins, +0.3195 family mean
CE gain, worst loss -0.3302. Its 4-case gate passed 4/4. The required 6/8
consistency gate stopped it. The cached half-strength reference was rejected
next (6/8 wins, worst loss -0.5483). Live rejection continuation passed.

[The saved-history numerical audit](audit/forward-v1/result.json) measured attached
versus deployed ordinary merging on four training histories, one per voice group.
The hidden mismatch is 55–64% of the adapter effect; top semantic choices differ
at 3.8–4.6% of generation positions. Both styled and unstyled examples are included.
This is a material numerical mismatch, not proof of an audio failure cause.
Exact Off restoration passed. The audit produced no new audio or trained candidate.

The next candidate uses positive-only semantic imitation of the existing chosen
training takes, a reference KL and prompt anchor, and a differentiable forward
that reproduces the deployed CPU FP32 merge before its single BF16 cast.
Its small-model and actual-host audits passed: exact hidden outputs and guided
semantic probabilities, real adapter gradients, and unchanged base weights.
The audit update was discarded. Host loading took 674.5 seconds under substantial
unrelated memory/IO activity. No audio was generated by these forward audits.

The acceptance campaign finished successfully: all three reference scorecards
were reproduced through the CLI, the Off PCM audit passed, the known checkpoint
was rejected, and the controller proceeded to the cached half-strength attempt.
Eight new quality clips and one engineering Off clip were generated in acceptance.
The engineering clip is not an independent quality observation.

The positive-imitation pilot completed 10 real optimizer updates in 202.6 seconds
of measured update time. Its exported adapter failed the first development stage:
2/4 wins, -0.0410 family mean CE, and -1.0503 worst delta versus Off. All four cases
scored below the half-strength starting point (mean -0.4264). The optimizer, RNG
state, ordinary adapter, raw audio and [scorecard](evaluations/eval-d85f5659f254bc60b5f7/scorecard.md)
are preserved. A 30-update continuation was prepared but not registered because
this screen did not justify more of the same signal.

The direct CE depth-gain pilot finished: both bounded patterns scored below Off
under the predeclared objective (-0.6021 and -0.1686). Off was selected. Eight
training clips were generated and preserved; no development candidate was rendered.
The [listening page](../../eval/listen/reward_game_v1_20260908/index.html) now includes
these training selections and their regressions separately from development.

The next method includes positive imitation of the seven residual codebooks as
well as semantic codes. The small decoder test passed, followed by an actual-host
audit: all eight codes reconstructed 501 saved feedback frames exactly, the mean
residual-policy KL against sequential prefix decoding was 0.000165 (limit 0.001),
and gradients updated only ordinary LoRA factors. Base and depth-decoder weights
remained frozen; the audit update was discarded. Actual BF16 probabilities were
not bit-identical (maximum log-probability difference 0.1746).

All 24 chosen training histories passed exact recovery (12,024 feedback frames).
The 10-update pilot completed and passed the first development gate: 3/4 wins,
+0.3181 family mean CE and -0.3786 worst delta. It then passed eight cases:
6/8 wins, +0.1746 mean CE and -0.3786 worst delta. Its fixed adapter is now
finished all 16 development cases: 11/16 wins, +0.0753 mean CE, worst -0.5877.
It was rejected against all three Off requirements. Against the original LoRA it
won 10/16 with +0.0797 mean CE. Fresh confirmation has not started. The complete relevant test suite passes 50 tests. It combines the richer objective with a learning rate of 1e-6; their causal
contributions will not be isolated by this combined experiment. No development
family supplies training tokens or labels.

No improvement is claimed. An empty method backlog requires supervising-agent
selection; it does not complete the search. Failed earlier studies remain
preserved and Off remains eligible. GPU batches run serially to bound host memory.
The studio is restored after borrowing GPU 0 for its declared cases.

The corrected zero-update numerical audit reproduces the cached reference exactly
and detects no training-mode mismatch. Its separate tensor-layout control explains
the discrepancy in the first audit; both versions and their results are preserved.

The next registered attempt is `attempt-d0986266be642e9a`: eight matched parent
versus Off takes on four training families, two seeds each. The fixed parent is the
11/16 residual-imitation checkpoint at its exported unit strength. Higher CE within
each pair chooses the target; exact ties choose Off. All labels freeze before exact
eight-code replay and 10 new positive semantic/residual imitation updates at 1e-6.
The objective and its preservation terms are unchanged. This is new training data
and a new starting policy, not fresh confirmation. The frozen development game
will evaluate the exported adapter through its normal ladder.

`audit/cumulative-cost-current.json` can be rebuilt with the `reward_game.costs`
module. It separates completed clips, updates, replay, loading, rendering, and
measured lease intervals; overlapping wall segments are not summed as GPU usage.

The matched-data checkpoint `dd7ca9b32bf68876936eb94941bac516bdd7f4102dabc16880dac829c2ba9dff`
failed its four-case development screen: 2/4 wins, mean -0.3083 CE, worst -2.9697
CE on instrumental dance. All four outputs were valid. Eight exact target replays,
10 real optimizer updates, and full state/export audits passed; these do not
establish musical improvement. Both sides of all training pairs are archived in
`attempts/attempt-d0986266be642e9a/matched-training/`.

Attempt `attempt-2fef1e8749204c12` now tests two fixed q/k/v/o gain patterns on the
11/16 residual-imitation parent, selected by direct CE on four training families.
It has an eight-training-clip budget; only a selected fixed adapter enters the
development ladder. Off remains eligible at zero. This is the second direct
gain-search pilot, after which another comparable failure calls for a different
mechanism. No candidate has qualified for fresh confirmation.

Direct projection gains also selected Off: objectives -0.3468 and -0.9859 for the
two patterns. Eight training clips, zero optimizer updates, and zero development
clips were used. The gain grid is closed after two rejected direct-search pilots.

Attempt `attempt-d21fc4af9b9a0b74` uses the same fixed residual-imitation parent and
all eight of its previously captured training takes. Each family supplies two
parent takes; centered, normalized CE gives signed advantages, with a predeclared
0.05 gap floor. A clipped ratio of selected per-frame semantic plus seven residual
code probabilities replaces positive imitation. Reference KL and prompt anchors
stay at 0.2, and a new AdamW optimizer runs 10 updates at 1e-6. Four targets reuse
exact caches and four require new exact replay; no training audio is added. This
is an untruncated code-policy surrogate, not exact deployment-sampler likelihood
or waveform likelihood. The fixed development gate decides its audio outcome.

The first signed clipped-policy attempt finished all 16 development cases: 12/16 wins, +0.134689 equal-family CE, worst -0.531847 versus Off. It was rejected on wins and the worst-loss floor. Versus the original adapter: 11/16 wins and +0.139017 CE. Confirmation remains unrendered. The expanded relevant test suite passes all 56 tests.

Attempt `attempt-01894958f3db39c0` now tests full-batch clipped optimization: all eight saved parent rollouts contribute equally before each of ten optimizer steps. The parent, data, advantages, objective, regularization and learning rate remain fixed; a new optimizer is used. It requires 80 rollout forward/backward evaluations and no new training audio. This is the second related signed-policy pilot, after which a failure requires another mechanism.

Fresh confirmation tooling is prepared in `reward_game.confirmation` and `reward_game.confirmation_intent`; it refuses candidates without a strict completed development pass. An unused eight-family draft with two new seeds passed exact content/seed checks over 4,425 prior JSON records. Fresh protocols and checkpoint bytes freeze before audio. Baseline-only diagnostic tolerances freeze before candidate diagnostics. A successful first fresh batch still requires independently frozen replication and a fixed-total-energy composition check.

Unused fresh-evidence helpers now include independent replication fixtures, `reward_game.composition` for matched reduced-style comparisons, and `reward_game.complete_confirmation`, which refuses packaging until two fixed-candidate fresh batches, preservation reviews, and the fixed-energy check pass. These helpers have not generated any audio or established an improvement.

A possible acoustic-host follow-up is recorded in `recipes/acoustic-mechanism-draft-v1.json`, conditional on rejection of the second signed-policy pilot. Its synthetic flow-block replay test passed, including a short overlap case. The actual acoustic capture/gradient audits are still unrun. The separate CE waveform helper reproduced its prior verified score and gradient on existing audio with no new rendered clips or optimizer updates.

The balanced signed-policy pilot failed its first four cases: 2/4 wins, +0.162187 mean CE and worst -0.237593 versus Off. Its ten full-batch updates took 1,607.4 seconds and evaluated all eight training rollouts per update (80 passes). Both signed-policy pilots are now closed.

The next selected mechanism is a separately typed rank-8 acoustic attention LoRA trained through direct CE waveform gradients. Before a training candidate is registered, one existing training Off recording must pass exact capture and truncated-flow replay parity, followed by a real acoustic-model gradient/ordinary-merge audit. The audit budget is one duplicate engineering recording and one discarded update. No acoustic quality candidate or fresh confirmation has yet been rendered.

The acoustic host audits passed: exact source PCM, exact replay of all five truncated flow chunks, exact waveform values with gradients enabled, a nonzero adapter gradient, and an ordinary merged-forward match after one discarded update. All eight training captures then passed exact PCM and replay checks. The separate acoustic game Off render also reproduced the original reference PCM exactly.

Acoustic attempt `attempt-cd23c91b67d1e7b7` is running eight updates from a fresh zero adapter at learning rate 0.001. Its objective is direct frozen CE plus a latent fidelity anchor, differentiating only the final two flow steps of each chunk. Earlier trajectory states and overlap stay fixed, making this a truncated derivative. The ordinary full generation ladder will decide whether it transfers. The pilot uses no new independent training draws. At launch the combined ledger records 87 newly rendered clips: 52 development, 24 training, and 11 duplicate engineering audits; 50 earlier candidate updates and three discarded audit updates. No confirmation has been rendered.

The acoustic pilot completed all eight optimizer updates in 726.7 seconds. Its export audit passed: 432 tensors match full state, 288 optimizer parameter states record eight steps, and Off restoration is exact. Checkpoint SHA-256 is `1a5134ede7e011d08ee9a9e3da3ab557302f93acf9df65e9c87fe8d394d56992`. It passed the first development stage with 4/4 wins, +0.129604 equal-family CE and a smallest gain of +0.052579. Evaluation continues to eight cases; this is not confirmation. [Separate acoustic listening page](../../eval/listen/acoustic-v1/index.html).

The completed acoustic candidate was rejected on the unchanged mean gate alone: 16/16 meaningful wins over Off, equal-family gain +0.095898 (required +0.10), and smallest gain +0.024003. Against the original: 10/16 wins and +0.100226 mean CE. Against half strength: 8/16 and -0.058561 mean CE. All raw results and the failed decision remain in the separate acoustic ledger.

Attempt `attempt-fc4e20db06661f0a` is a single bounded global calibration: fold a factor of 1.5 into all acoustic up factors, then use runtime multiplier 1. It adds zero optimizer updates and zero training draws. Zero and unit folding checks passed on all 432 tensors. New checkpoint SHA-256 is `46182bdf40689487f34a2e3b48a53540d1a259da6de5f9e91f8c6bea88b14ee2`. This is exposed development calibration; it still requires the full fixed ladder before fresh confirmation. A second failure closes this acoustic pilot family. Cumulative completed work before this calibration evaluation is 103 new clips and 58 candidate optimizer updates; three additional audit updates were discarded.

The fixed calibrated adapter passed all 16 development cases with 16/16 meaningful wins, +0.136709 mean CE and a smallest gain of +0.021300 versus Off. Against the original: 11/16 wins and +0.141037 mean CE. Against half strength: 10/16 wins and -0.017750 mean CE. Its first fresh batch `acoustic-gain150-first-v1` freezes eight new families, two seeds, three arms and the unchanged practical/uncertainty criteria. A separate replication and preservation/composition evidence are still required.

The first fresh job stopped after three valid draws because the consumer expected compact-JSON provenance hashes while the ordinary renderer uses default JSON separators. The `manifest-digest-v1` amendment preserves the original protocol, source snapshots, completed observations and raw recordings; it corrects only provenance validation. Off restoration passed. The resumed service reuses all three completed draws and continues unattempted cases. Actual `SearchRenderer.observe` integration tests now cover both LM/acoustic confirmation and composition; all 16 relevant tests passed in 1.50 seconds. No render, reward, seed, candidate or gate changed.

The first fresh acoustic batch completed 48 valid recordings but failed confirmation. It won meaningfully on all 16 cases versus Off; mean +0.084564 missed +0.10. The Off family interval was [0.067051, 0.108237]. Against the original it won 8/16 with +0.114078 mean and interval [-0.133661, 0.355353]. Off restoration passed. This batch is now exposed; no preservation or replication was started.

The next fixed candidate combines the strongest signed LM policy and the calibrated acoustic correction, each as its own ordinary LoRA at unit strength. It uses a separately declared `joint-v1` game and a fixed pair manifest. It adds no optimizer updates or training audio. The actual native pair merge/Off audit must pass before its four-case screen. [Current running state](RUNNING.md).

The joint audit passed exact Off PCM, all 288 native merged projections, and exact Off restoration. Its completed development result passed every fixed gate: 14/16 meaningful wins, +0.218012 mean CE and worst -0.409758 vs Off; 11/16 and +0.222339 vs original; 10/16 and +0.063553 vs half. The worst half-reference loss is -1.017940. Both Off losses and every comparison remain in the [joint listening page](../../eval/listen/joint-v1/index.html).

The fixed pair has entered a new first fresh batch, `joint-v1/confirmation/joint-policy-acoustic-first-v1`, using eight previously unused families and seeds 99139/104729. Its protocol SHA is `11ee66b2a5f7666537c6e980a3c1d3abb9f5e1b598535ef46060d3dfc5662410`. The failed acoustic fresh batch is excluded. Forty-eight new recordings are declared on GPU 1; all original practical gates and whole-family uncertainty rules remain fixed. A passing first batch still requires an independent eight-family replication, preservation diagnostics and matched fixed-energy composition. No improvement has yet been confirmed.

The joint first fresh batch finished and failed: 15 valid comparisons, one candidate short output (17.415 seconds), 11 wins and observed mean +0.078545 vs Off; 12 wins and +0.184946 vs original. No family interval is issued for the incomplete batch. Exact Off restoration passed. The next selected intervention changes trainable acoustic support to 72 feed-forward projections, preserving zero attention deltas in a native 648-tensor block export; its actual gradient/native merge audit is running. See RUNNING.md for the prepared sources and next steps.

## September 9 playback repair and preparation during FF training

The user reported broken joint and acoustic WAV URLs. The recordings existed; the separate listening server rejected their explicit links into the analysis tree and redirected audio requests to a directory. A narrow server repair permits individual analysis WAV links. The previous source and log, new service definition, HTTP checks and exact downloaded hashes are retained in `amendments/listen-audio-links-v1`. All 64 joint and 80 acoustic development audio URLs now return HTTP 200. One HTTP regression test passed. Frozen generation/training sources and original recordings were unchanged.

The completed fresh pages are published at `http://192.168.1.90:8888/acoustic-fresh-v1/` and `http://192.168.1.90:8888/joint-fresh-v1/`; all 96 recording links passed HTTP checks. Use `python -m conceptmod.textsliders.reward_game.publish_confirmation --home CHILD_HOME --batch BATCH --name LISTEN_FOLDER` to publish another existing batch. It verifies raw files against observations and copies the generated page unchanged. Both current batches remain failed.

The separate, unselected merged-weight cache prototype passed 12 CPU tests, including two optimizer steps on actual reduced-size 36-block Music 3 transformers. Values, factor/input gradients and updated state match the old implementation; the cache invalidates for state loads, Parameter replacement, styles and multipliers. The active FF pilot continues with its original frozen implementation. `audit_cached_acoustic.py` prepares a zero-update full-size equivalence audit for after GPU 1 is available; it has not run. No music improvement or measured full-size speedup is claimed.

Unused `recipes/independent-bank-b-draft-v1.json` was authored before any FF development recordings. It adds eight balanced voice families and seeds 196613/262147, passing exact novelty checks across 5041 existing JSON records. No candidate or fresh protocol is assigned to it.

### Acoustic block composition bridge (September 9)

The separate `block-v1` game evaluates ordinary full-block acoustic LoRAs with both attention and feed-forward factors. Candidate `ae49598815a2c0df` uses exact factor copies from attention gain150 and FF step eight on disjoint projections, at one fixed unit multiplier. The native GPU audit verified all 216 merged projections, both captured branch outputs, zero and Off restoration. Its own complete Off recording reproduced the original decoded PCM exactly. This is a registered checkpoint construction with zero new optimizer updates; its two parents share the same eight captured training examples.

The fixed development order, controls, physical GPUs and CE gates are unchanged. The first four cases passed; further development is active. [Listen to the current development page](http://192.168.1.90:8888/block-v1/). The reusable commands are:

```bash
/home/mikkel/anaconda3/envs/minimax-music3/bin/python -m conceptmod.textsliders.reward_game.block \
  --home analysis/reward_game_v1_20260908/block-v1 leaderboard
/home/mikkel/anaconda3/envs/minimax-music3/bin/python -m conceptmod.textsliders.reward_game.block \
  --home analysis/reward_game_v1_20260908/block-v1 inspect --run eval-1d60eebe79ac75da4077
```

`block_confirmation`, `block_intent_locked`, `block_composition` and `block_complete_confirmation` provide the corresponding single-file confirmation path. Twenty-six relevant tests passed. These helpers reject a partial development result and require independent replication and preservation before packaging. Their template and two unused fixture banks are prepared, but no fresh block protocol or recording exists yet. [Current state](RUNNING.md) records the active service and source freezes.
